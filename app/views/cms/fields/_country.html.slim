
- existing_countries = Country.pluck(:country_code)
- countries = ISO3166::Country.translations.map { |k,v| [v, k] unless existing_countries.include?(k) }.compact

.hint Only countries which haven't already been added are listed below.
= f.input :country_code, label: false, prompt: 'Choose a country', collection: countries, input_html: { class: 'search' }
