
.item
  = model_icon Venue
  .content
    - if policy(venue).show?
      a.header href=url_for([:cms, venue]) = venue.label
    - else
      .header = venue.label
    .description #{venue.address_text} - #{translate_model_count(venue.events)}