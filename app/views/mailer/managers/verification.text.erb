<%= translate('mail.verification.intro', event: (@event.name || @event.venue.street), date: @event.updated_at.to_s(:long)) %>

<%= translate('mail.verification.purpose') %>

<%= translate('mail.verification.escalate.title') %>
<%= '=' * translate('mail.verification.escalate.title').length %>
<%= translate('mail.verification.escalate.description', region: @event.venue.parent.label, escalates_in_days: @event.escalates_in_days_in_words, expires_in_days: @event.expires_in_days_in_words) %>

<%= render 'mailer/managers/event', event: @event %>

<%= translate('mail.common.actions') %>
<%= '=' * translate('mail.common.actions').length %>
<%= translate('mail.verification.call_to_action') %>
<%= translate('mail.verification.confirm') %> - <%= @magic_link %>
<%= translate('mail.verification.update') %> - <%= @magic_link %>


<%= message translate('mail.common.about'), :text %>

<%= message translate('mail.common.alert'), :text, :alert %>