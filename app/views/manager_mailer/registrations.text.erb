
There have been <%= @registrations.count %> registrations in since <%= @since.to_s(:long) %>

<% @registrations.each do |registration| %>
  <%= registration.name %> - <%= registration.email %>
  <%= registration.created_at.to_s(:long) %>
  <%= registration.comment || '(No Comment)' %>
  
<% end %>
